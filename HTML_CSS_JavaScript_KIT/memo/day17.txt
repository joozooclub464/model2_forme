함수(function)
	자바스크립트에서는 함수도 하나의 값이다.
	따라서 함수를 변수에 대입할 수 있다.
	다른 함수 내에 중첩되어 정의할 수 있다.
	
함수 선언 방법
	function 함수명(매개변수1, 매개변수2, ...) {
		실행할 문장;
		return 리턴값;
	}

함수 표현식
	let 함수명 = function(매개변수1, 매개변수2, ...) {
		실행할 문장;
		return 리턴값;
	}

	함수명();

